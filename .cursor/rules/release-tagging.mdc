---
description: 版本号、开发标签（-dev）与正式验收标签的流程（crypto-quant）
alwaysApply: true
---

# 发版与标签约定（Crypto Quant）

## `VERSION` 文件

- **`VERSION` 只反映「已口头验收」的正式版本**（以仓库根目录 `VERSION` 文件当前内容为准）。
- **在负责人明确说「VX.Y.Z 验收通过」之前，不得把 `VERSION` 改成下一正式号**（例如未验收前不得改为 `0.0.5`）。

## 开发过程标签（每轮新开发要打，`-dev` 方式）

- 每完成一批可交付的新开发（或每轮迭代结束），应在本机 Git 打一个**开发检查点标签**，格式：
  - 朝 **V0.0.5** 推进时：**`v0.0.5-dev.1`、`v0.0.5-dev.2` …**
  - 其中 **主版本号与下一目标正式版一致**；**`-dev.N`** 为开发快照序号，递增即可。
- 标签名使用小写 `v` 前缀；说明里可写「开发检查点 / 待验收 V0.0.5」等。
- 完成开发后：**提示负责人**执行（或在其授权且具备 git 权限时协助执行）：
  - `git tag -a v0.0.5-dev.N -m "开发检查点：…"`
  - 正式验收前**不要**用 `./scripts/git-tag-release.sh` 打 `-dev` 标签（脚本面向正式版）；开发标签用裸 `git tag -a` 即可。

## 正式验收标签

- 仅当负责人明确说 **「V0.0.5 验收通过」**（或等价表述）时：
  1. 将根目录 **`VERSION`** 更新为 `0.0.5`；
  2. 再执行正式提交与 **`v0.0.5`** 带注释标签（可用 `./scripts/git-tag-release.sh v0.0.5 "V0.0.5 验收通过"`）。

### Agent 义务（负责人口头验收时）

- **触发语**：负责人只要说出 **「验收通过」**（或 **「VX.Y.Z 验收通过」**、**「本版本验收通过」** 等明确等价表述），即视为正式验收流程已触发。
- **版本号**：优先采用话术中的 **X.Y.Z**；若未点名版本，则以当前对话已约定的目标版本为准；仍无法确定时，读取根目录 **`VERSION`** 并与负责人核对，**不得猜打错标签**。
- **必须执行**（在具备 `git_write`、且负责人未禁止改 Git 的前提下）：
  1. 确认 **`VERSION`** 与将打的 **`vX.Y.Z`** 一致；若负责人已口头验收但 `VERSION` 未改，**先更新 `VERSION` 再操作**。
  2. 在**当前待发布状态**的提交上（一般为 `HEAD`，除非负责人指定某 commit）创建**附注标签** **`vX.Y.Z`**，说明与验收用语一致，例如：  
     `./scripts/git-tag-release.sh vX.Y.Z "VX.Y.Z 验收通过"`  
     或等价：`git tag -a vX.Y.Z -m "VX.Y.Z 验收通过"`（若无需脚本里的 `git add -A` / `commit` 行为，可仅用 `git tag -a`，但须保证标签落在正确提交上）。
  3. 若标签 **已存在**，不要覆盖；向负责人说明并确认是否要打在新提交上或改用新补丁版本。
- **推送**：是否 `git push` / `git push origin vX.Y.Z` 由负责人决定；Agent 可在打标后提示待推送命令。

## 与 README 的关系

- 详细说明以仓库根目录 **`README.md` →「Git 与发版约定」** 为准；本规则与 README 冲突时以**负责人最新口头约定**为准。
